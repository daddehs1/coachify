import styled from 'styled-components';

const SelectViewWrapper = styled.div `
`

const FixedArea = styled.div `
`

const DynamicArea = styled.div `
`

export {
  SelectViewWrapper,
  FixedArea,
  DynamicArea
};
